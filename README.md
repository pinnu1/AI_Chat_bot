# AI_Chat_bot
chat bot using the langchain and langgraph  using tools which is used to search the web_broweser 
for run the app make .env file in the copy folder 
add the Google_API_KEY 
and to run 
run the cammnd streamlit run streamlit_frontend.py
